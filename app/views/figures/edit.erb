EDIT FIGURE FORM


<form action="/figures/<%= @figure.id %>" method="post">

  Current Name: <%= @figure.name %>
  Edit Figure Name:
  <input type="text" name="figure[name]" id="figure_name">    

  Select From Existing Landmarks:
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>"><%= landmark.name %></input>
  <% end %>

  And/Or Create a New Landmark:
  <input type="text" name="landmark[name]" id="new_landmark">


  Select From Existing Titles:
  <% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>"><%= title.name %></input>
  <% end %>

  And/Or Create a New Title:
  <input type="text" name="title[name]" id="new_title">

  <input type="submit" name="submit" id="Edit Figure">Edit Figure</input>


</form>